// Package mails builds this application's email.
//
// A mail function returns a ready mail.Message. Keeping them here rather than
// inline in a handler means the wording lives in one place and a handler stays
// about what happened, not about how to phrase it.
package mails
