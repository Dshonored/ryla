See [AGENTS.md](AGENTS.md).

That file is the single source of truth for how to work in this project: the
commands, which files are generated, and the conventions worth preserving.
Keeping one copy avoids the two drifting apart.
